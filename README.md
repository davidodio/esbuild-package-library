# Component library

1. Please take a look, not sure how to get this working / building correctly.

1st, for `ui-components`

```
cd ui-components && npm i
rm -rf ./dist && npm build
```

2nd, in for `dashboard-service`

```
cd dashboard-service && npm i
npm run dev
```
